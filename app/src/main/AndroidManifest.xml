<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bootcampsns">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.SplashActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.LoginActivity" />
        <activity
            android:name=".activity.ServerSettingsActivity"
            android:label="@string/title_activity_server_settings" />
        <activity android:name=".activity.RegisterActivity" />
        <activity android:name=".activity.TimelineActivity" />
        <activity android:name="jp.example.mainecfluffy.MaineCFluffyLoginActivity" />
        <activity android:name="jp.example.mainecfluffy.MaineCFluffyConfirmActivity" />
        <activity android:name=".activity.UploadActivity" />
        <activity android:name=".activity.FriendActivity"></activity>
    </application>

</manifest>